---
deployment:
  tasks:
    - export DEPLOYPATH=/home/ravenscv/fuzzysolution.com/triage
    - /bin/cp -R .htaccess *.php *.css *.js *.json *.txt *.png $DEPLOYPATH/ 2>/dev/null || true
    - /bin/cp -R migrations $DEPLOYPATH/ 2>/dev/null || true
    - /bin/cp -R scrubs $DEPLOYPATH/ 2>/dev/null || true


